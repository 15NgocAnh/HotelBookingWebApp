@page "{id:int}"
@model HotelBooking.Web.Pages.ExtraCategories.DetailsModel
@{
    ViewData["Title"] = "Extra Category Details";
    Layout = "_Layout";
}

<div class="container-fluid px-4 py-4">
    <div class="card shadow-sm">
        <div class="card-header bg-white py-3">
            <div class="row align-items-center">
                <div class="col">
                    <h4 class="mb-0 text-primary">
                        <i class="fas fa-info-circle me-2"></i>Extra Category Details
                    </h4>
                </div>
            </div>
        </div>
        <div class="card-body">
            @if (TempData["ErrorMessage"] != null)
            {
                <div class="alert alert-danger" role="alert">
                    @TempData["ErrorMessage"]
                </div>
            }

            <dl class="row">
                <dt class="col-sm-2">Name</dt>
                <dd class="col-sm-10">@Model.ExtraCategory.Name</dd>
            </dl>

            <div>
                <a asp-page="./Edit" asp-route-id="@Model.ExtraCategory.Id" class="btn btn-primary">
                    <i class="fas fa-edit me-1"></i>Edit
                </a>
                <a asp-page="./Index" class="btn btn-secondary">
                    <i class="fas fa-arrow-left me-1"></i>Back to List
                </a>
            </div>
        </div>
    </div>
</div> 